import { useMemo, useState } from "react";
import { FlatList, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { AppHeader, EmptyState, PrimaryButton, SectionTitle } from "@/components/app-ui";
import { convertToKg, CropName, formatCurrency } from "@/lib/farm-data";
import { useFarm } from "@/lib/farm-context";
import { exportCsv, exportPdf } from "@/lib/export-data";

const cultures: CropName[] = ["Banana", "Batata-doce", "Abobrinha verde"];

export default function AnalyticsScreen() {
  const { state } = useFarm(); const [feedback, setFeedback] = useState("");
  const expenses = state.transactions.filter((item) => item.kind === "expense"); const revenue = state.transactions.filter((item) => item.kind === "revenue");
  const totalExpenses = expenses.reduce((sum, item) => sum + item.amount, 0); const totalRevenue = revenue.reduce((sum, item) => sum + item.amount, 0);
  const cashFlow = useMemo(() => { const buckets = Array.from({ length: 6 }, (_, index) => { const date = new Date(); date.setMonth(date.getMonth() - (5 - index)); return { key: `${date.getFullYear()}-${date.getMonth()}`, label: date.toLocaleDateString("pt-BR", { month: "short" }).replace(".", ""), income: 0, expense: 0 }; }); state.transactions.forEach((item) => { const date = new Date(item.date); const bucket = buckets.find((candidate) => candidate.key === `${date.getFullYear()}-${date.getMonth()}`); if (bucket) item.kind === "revenue" ? bucket.income += item.amount : bucket.expense += item.amount; }); return buckets; }, [state.transactions]);
  const maxCash = Math.max(1, ...cashFlow.flatMap((item) => [item.income, item.expense]));
  const cultureMetrics = cultures.map((culture) => { const cultureRevenue = revenue.filter((item) => item.culture === culture); const cultureExpenses = expenses.filter((item) => item.culture === culture); const income = cultureRevenue.reduce((sum, item) => sum + item.amount, 0); const cost = cultureExpenses.reduce((sum, item) => sum + item.amount, 0); const kg = cultureRevenue.reduce((sum, item) => sum + (item.quantityKg ?? convertToKg(item.quantity, item.unit, item.kgPerUnit) ?? 0), 0); const margin = income - cost; return { culture, income, cost, kg, avgPrice: kg > 0 ? income / kg : null, margin, marginPercent: income > 0 ? margin / income * 100 : null }; });
  const maxCost = Math.max(1, ...cultureMetrics.map((item) => item.cost));
  async function runExport(kind: "csv" | "pdf") { try { setFeedback("Preparando arquivo…"); kind === "csv" ? await exportCsv(state) : await exportPdf(state); setFeedback(`${kind.toUpperCase()} pronto para compartilhar.`); } catch (error) { setFeedback(error instanceof Error ? error.message : "Não foi possível exportar agora."); } }
  return <ScreenContainer className="px-5 pt-6" edges={["top", "left", "right"]}><FlatList data={["analytics"]} keyExtractor={(item) => item} renderItem={() => null} showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 32 }} ListHeaderComponent={<View><AppHeader eyebrow="ANÁLISES DO SÍTIO" title="Veja para onde vai o dinheiro" subtitle="Compare custo, preço médio por kg e margem de cada cultura." /><View className="mb-5 flex-row gap-2"><View className="flex-1 rounded-2xl border border-border bg-surface p-4"><Text className="text-xs text-muted">Receitas</Text><Text className="mt-1 text-lg font-bold text-success">{formatCurrency(totalRevenue)}</Text></View><View className="flex-1 rounded-2xl border border-border bg-surface p-4"><Text className="text-xs text-muted">Custos</Text><Text className="mt-1 text-lg font-bold text-error">{formatCurrency(totalExpenses)}</Text></View></View><SectionTitle title="Fluxo de caixa · 6 meses" />{state.transactions.length ? <View className="mb-6 rounded-2xl border border-border bg-surface p-4"><View className="mb-3 flex-row justify-end gap-4"><View className="flex-row items-center"><View className="mr-1.5 h-2 w-2 rounded-full bg-success" /><Text className="text-xs text-muted">Entradas</Text></View><View className="flex-row items-center"><View className="mr-1.5 h-2 w-2 rounded-full bg-error" /><Text className="text-xs text-muted">Saídas</Text></View></View><View className="h-44 flex-row items-end justify-between gap-2">{cashFlow.map((item) => <View key={item.key} className="flex-1 items-center"><View className="h-36 w-full flex-row items-end justify-center gap-1"><View className="w-2 rounded-t-md bg-success" style={{ height: Math.max(item.income ? 5 : 0, 120 * item.income / maxCash) }} /><View className="w-2 rounded-t-md bg-error" style={{ height: Math.max(item.expense ? 5 : 0, 120 * item.expense / maxCash) }} /></View><Text className="mt-2 text-[10px] capitalize text-muted">{item.label}</Text></View>)}</View></View> : <View className="mb-6"><EmptyState icon="bar-chart" title="Sem dados para o gráfico" body="Registre receitas e despesas para acompanhar a evolução mensal." /></View>}<SectionTitle title="Desempenho por cultura" />{state.transactions.length ? <View className="mb-6">{cultureMetrics.map((item) => <View key={item.culture} className="mb-3 rounded-2xl border border-border bg-surface p-4"><View className="mb-3 flex-row items-center justify-between"><Text className="font-bold text-foreground">{item.culture}</Text><Text className={`font-bold ${item.margin >= 0 ? "text-success" : "text-error"}`}>{item.margin >= 0 ? "Margem " : "Prejuízo "}{formatCurrency(Math.abs(item.margin))}</Text></View><View className="mb-3 h-2 overflow-hidden rounded-full bg-background"><View className={`h-full rounded-full ${item.margin >= 0 ? "bg-success" : "bg-error"}`} style={{ width: `${Math.min(100, Math.max(4, item.income ? Math.abs(item.margin) / item.income * 100 : 0))}%` }} /></View><View className="flex-row flex-wrap gap-y-3"><View className="w-1/2"><Text className="text-xs text-muted">Preço médio/kg</Text><Text className="mt-1 font-bold text-foreground">{item.avgPrice === null ? "R$ —" : formatCurrency(item.avgPrice)}</Text></View><View className="w-1/2"><Text className="text-xs text-muted">Margem</Text><Text className={`mt-1 font-bold ${item.margin >= 0 ? "text-success" : "text-error"}`}>{item.marginPercent === null ? "—" : `${item.marginPercent.toFixed(1).replace(".", ",")}%`}</Text></View><View className="w-1/2"><Text className="text-xs text-muted">Receita</Text><Text className="mt-1 font-semibold text-foreground">{formatCurrency(item.income)}</Text></View><View className="w-1/2"><Text className="text-xs text-muted">Custo</Text><Text className="mt-1 font-semibold text-foreground">{formatCurrency(item.cost)}</Text></View><View className="w-full"><Text className="text-xs text-muted">Volume vendido</Text><Text className="mt-1 font-semibold text-foreground">{item.kg ? `${item.kg.toLocaleString("pt-BR")} kg` : "Sem conversão de kg"}</Text></View></View></View>)}</View> : <View className="mb-6"><EmptyState icon="agriculture" title="Sem dados por cultura" body="Associe receitas e custos à banana, batata-doce ou abobrinha verde." /></View>}<SectionTitle title="Custos por cultura" />{state.transactions.length ? <View className="mb-6 rounded-2xl border border-border bg-surface p-4">{cultureMetrics.map((item) => <View key={item.culture} className="mb-4"><View className="mb-1 flex-row justify-between"><Text className="text-sm font-semibold text-foreground">{item.culture}</Text><Text className="text-sm font-bold text-foreground">{formatCurrency(item.cost)}</Text></View><View className="h-3 overflow-hidden rounded-full bg-background"><View className="h-full rounded-full bg-primary" style={{ width: `${Math.max(item.cost ? 4 : 0, 100 * item.cost / maxCost)}%` }} /></View></View>)}<Text className="mt-1 text-xs leading-4 text-muted">Associe cada despesa a uma cultura para detalhar custos e margem.</Text></View> : null}<SectionTitle title="Exportar dados" /><View className="rounded-2xl border border-border bg-surface p-4"><Text className="text-sm leading-5 text-muted">Inclui unidades, quantidades convertidas, preço/kg, margem e inventário com validade.</Text><View className="mt-4 flex-row gap-2"><View className="flex-1"><PrimaryButton label="Baixar CSV" icon="file-download" onPress={() => runExport("csv")} /></View><View className="flex-1"><PrimaryButton label="Gerar PDF" icon="picture-as-pdf" onPress={() => runExport("pdf")} /></View></View>{feedback ? <Text className="mt-3 text-center text-xs font-semibold text-primary">{feedback}</Text> : null}</View></View>} /></ScreenContainer>;
}
